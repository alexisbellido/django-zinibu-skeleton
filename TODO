- Upgrade to work with Django 1.9. Use application created by django-admin startapp as example.

- Create some basic Django functionality to see this application working from a project: management commands, views, models, admin changes, signals, forms, templates and template tags, some static files to be used, middleware, URLs, etc.
